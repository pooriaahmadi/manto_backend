export enum logMethods {
  INFO = 1,
  ERROR = 2,
  LOAD = 3,
  DEBUG = 4,
  READY = 5,
}

export enum FolderModes {
  PUBLIC = 0,
  PRIVATE = 1,
  PASSWORD = 2,
}
export enum CommentModes {
  NEUTRAL = 0,
  POSITIVE = 1,
  NEGATIVE = 2,
}
